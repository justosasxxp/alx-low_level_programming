#include <stdio.h>

/**
 *simple callculation functions
 *
 *return "result"
 */

int add(int x, int y)
{
        return(x + y);
}


/**
 * subtraction lol
 */

int sub(int x, int y)
{
	return(x - y);
}


/**
 * multiplication
 */

int mul(int x, int y)
{
	return(x * y);
}

/**
 * division
 */

int div(int x, int y)
{
	return(x / y);
}

/**
 * modulus
 */

int mod(int x, int y)
{
	return(x % y);
}


