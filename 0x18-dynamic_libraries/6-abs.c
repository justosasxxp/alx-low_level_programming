#include "main.h"


/**
 *_abs - is a function that checks if character alphabet
 *
 *@n: takes in int c and checks
 *
 *Return: Always return 0 on success
 */
int _abs(int n)
{
	return (n >= 0 ? n : n * -1);
}
